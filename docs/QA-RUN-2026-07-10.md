# Admin HQ QA Run Log — 2026-07-10

## Request interpreted

Run a production-like QA hardening cycle without using production data, sensitive data, destructive actions or deployment.

## Settings

| Setting | Value |
|---|---|
| Data type | Sanitized synthetic local fixture |
| Production-like mode | `NODE_ENV=production` in regression runner |
| Systems covered | Invoice, Sales, Client, Property, Practice, Member |
| Local fixture scale | 240 records per system / 1,440 records total |
| Storage model | Browser localStorage, per-system keys |
| Deployment | Not triggered |
| Sensitive data | Not used |
| Destructive actions | Not performed |

## Inventory created

Full user-facing inventory is documented in:

- `docs/QA-INVENTORY-2026-07-10.md`

It covers:

- user contexts / roles
- routes
- buttons
- inputs
- modal
- states
- workflows
- acceptance criteria
- finite risk-based edge cases

## Regression coverage added

Files added/updated:

- `tests/fixtures/sanitized-production-scale-data.mjs`
- `tests/run-tests.mjs`

The test runner now validates:

- all required route files exist
- all six system definitions exist
- each system has enough fields and workflow statuses
- each sample dataset has 10 records
- each sample dataset returns 7 Pass / 3 Fail
- expected sample failing records match exactly
- report totals match validation output
- CSV round-trip preserves row count and first ID
- sanitized production-scale fixture creates 240 records per system
- production-scale records have deterministic unique IDs
- production-scale records contain no email-like data
- production-scale records contain no SA-ID-like 13-digit values
- production-scale failures are deterministic
- production-scale reports return 168 Pass / 72 Fail per system
- production-scale CSV round-trip preserves row count and first ID

## Bugs / issues found and fixes

### BUG-001 — Last three systems were public-facing but not live app systems

Evidence:

- Homepage had Property, Practice and Member positioned as future/next systems before the last-three go-live work.
- Admin HQ launcher previously showed only the first three systems.

Fix:

- Added `app/property-admin/index.html`.
- Added `app/practice-admin/index.html`.
- Added `app/member-admin/index.html`.
- Updated `app/index.html` to show six systems.
- Updated homepage and Admin Systems page to link all six systems.

Regression:

- `tests/run-tests.mjs` now asserts all six route files exist.

### BUG-002 — System navigation exposed only the first three systems

Evidence:

- Existing Invoice/Sales/Client pages had nav links only for Invoice, Sales and Client.

Fix:

- Updated Invoice, Sales and Client pages to expose all six systems.
- New Property, Practice and Member pages were created with all six nav links.

Regression:

- Route inventory now includes all six app pages.

### BUG-003 — Test coverage only validated first three systems

Evidence:

- Previous `tests/run-tests.mjs` expected only invoice, sales and client sample results.

Fix:

- Expanded `tests/run-tests.mjs` to include property, practice and member.
- Added deterministic expected failures for all six systems.
- Added production-scale fixture checks.

Regression:

- Regression runner now covers all six systems and 1,440 sanitized production-scale records.

### BUG-004 — Placeholder email CTA risk

Evidence:

- Homepage CTA previously contained `hello@example.com` during earlier page polish work.

Fix:

- Replaced placeholder mailto with internal `admin-systems.html#pilot` route.
- Repo search was previously run for `hello@example.com`; no stale result surfaced after cleanup.

Regression:

- Manual repo search should be rerun before release.

### BUG-005 — No documented acceptance criteria for user-facing features

Evidence:

- Product had routes and tests, but no full user-facing inventory tying features to acceptance criteria and finite edge cases.

Fix:

- Added `docs/QA-INVENTORY-2026-07-10.md`.

Regression:

- Future QA passes should update the inventory when routes, buttons, inputs, modal behavior, states or workflows change.

## Shared causes and dependencies

| Cause | Dependency | Fix strategy |
|---|---|---|
| First three systems matured before the last three | Launcher, nav, homepage and tests had asymmetric coverage | Move all systems through the same shared engine, shared controller and shared tests. |
| Static app uses dynamic shared engine | Any new system must update engine, route, nav, launcher and tests together | Regression runner now asserts system definitions and route files for all six. |
| No backend yet | Browser localStorage can only simulate pilot workflows | Keep production data blocked until Supabase Auth, DB, RLS and audit logs exist. |
| Vercel not redeployed from latest GitHub commits | Live public alias may lag repo | Stop at blocked handoff until redeploy/live browser QA. |

## Execution status

The repository now contains the production-scale fixture generator and regression runner.

This tool session could not execute the final runner against a freshly cloned latest repo because outbound GitHub network resolution failed in the container:

```text
fatal: unable to access 'https://github.com/TheRealButter/The-Admin-Department.git/': Could not resolve host: github.com
```

## Required handoff command

Run from a fresh checkout of latest `main`:

```bash
NODE_ENV=production node tests/run-tests.mjs
```

Expected clean result:

```text
All Admin HQ inventory and production-scale regression tests passed.
```

## Stop condition

Status: **blocked handoff**, not production clean pass.

Reason:

- Latest GitHub code is committed.
- Regression runner is committed.
- Production-scale sanitized fixture is committed.
- Live browser QA and final test execution require a fresh checkout / CI / Vercel deployment refresh.

No production deployment, production data access, sensitive data use, or destructive action was performed.
